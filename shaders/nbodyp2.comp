#version 450

#include "lib/nbodycommon.comp"

// second pass of nbody simulation
void main() {
    uint index = gl_GlobalInvocationID.x;
    if (index >= ubo.particleCount) return;	

    particles[index].pos += ubo.delta * particles[index].vel;
}

