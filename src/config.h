#ifndef CONFIG_H
#define CONFIG_H

const struct {
    const char* name = "Vulkanized Compute Showdown";
    struct {
        const int major = 0;
        const int minor = 0;
        const int patch = 1;
    } version;
} ProgramInfo;


#endif
